datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Webtoon {
  id          Int      @id @default(autoincrement())
  title       String
  genre       String
  releaseDate DateTime
  chapters    Int
  description String
}
